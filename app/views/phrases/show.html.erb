<%= @phrase.slang %>
<%= @phrase.description %>
<%= @phrase.example %>




<%= button_to "That's dope!", phrase_path(@phrase, params.merge(vote: "up")), action: :update, method: :patch %>
<%= button_to "That's whack!", phrase_path(@phrase), action: :update, method: :patch  %>

<%= link_to "Home", phrases_path %>

<%= @score %>
